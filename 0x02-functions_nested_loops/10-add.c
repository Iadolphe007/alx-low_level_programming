#include "main.h"
/**
 * add - function adding to number
 * @a: first number
 * @b: second number
 * Return: sum of the two number
 */

int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
