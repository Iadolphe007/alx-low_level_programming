#include "main.h"

/**
 * mul- function returning multiple of two number
 * @a: first character
 * @b: second character
 * Return: product of the two number
 */

int mul(int a, int b)
{
	int product;

	product = a * b;

	return (product);
}
